#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/tsx$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/tsx$exe"  "$basedir/../@sap/cds-dk/bin/cds-tsx.js" $args
  } else {
    & "$basedir/tsx$exe"  "$basedir/../@sap/cds-dk/bin/cds-tsx.js" $args
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
    $input | & "tsx$exe"  "$basedir/../@sap/cds-dk/bin/cds-tsx.js" $args
  } else {
    & "tsx$exe"  "$basedir/../@sap/cds-dk/bin/cds-tsx.js" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
